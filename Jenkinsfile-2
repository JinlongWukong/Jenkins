pipeline {
    agent {
        docker { image 'node:16.13.1-alpine' }
    }
    environment {
        PATH="/usr/local/bin:${env.PATH}"
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
